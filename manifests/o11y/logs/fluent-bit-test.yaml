apiVersion: v1
kind: Pod
metadata:
  name: fluent-bit-test
  namespace: bookinfo
  labels:
    app: fluent-bit-test
spec:
  containers:
  - name: ubuntu
    image: ubuntu:latest
    command: ["/bin/sh", "-c"]
    args:
      - |
        echo '{"key": "value"}'
  restartPolicy: Never